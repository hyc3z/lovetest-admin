apiVersion: v1
kind: Service
metadata:
  name: lovetest-admin-service
  labels:
    app: lovetest-admin
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: 80
    protocol: TCP
    name: http
  selector:
    app: lovetest-admin